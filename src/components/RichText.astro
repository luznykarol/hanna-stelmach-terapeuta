---

interface Props {
  html: string;
  class?: string;
}

const { html, class: className = '', ...rest } = Astro.props;
---

<div
  class:list={[
    'font-body text-base md:text-[22px] leading-[1.32] font-normal tracking-[-0.03em] richtext',
    className,
  ]}
  {...rest}
  set:html={html}
/>
