---
import { getContent } from '../lib/content';
const { footer, site, editable } = await getContent();

const year = 2026;
---

<footer class="bg-paper text-ink" {...editable.siteSettings}>
  <div
    class="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 px-5 py-8 sm:flex-row sm:px-8"
  >
    <span class="font-display text-lg">{site.logoLine1}</span>
    <span class="text-sm text-ink-soft">{footer.text}</span>
    <span class="text-xs text-ink-soft">© {year}</span>
  </div>
</footer>
